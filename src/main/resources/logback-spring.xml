<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <property name="APP_NAME" value="info-resign"/>
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [traceId:%mdc{trace_id}] [%thread] %-5level %logger{50} - %msg%n"/>

    <!-- 控制台输出（带颜色） -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %logger{50} - %replace(%msg){'(?m)^==>.*','\033[34m$0\033[0m'}%n
            </pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="Console"/>
    </root>



</configuration>